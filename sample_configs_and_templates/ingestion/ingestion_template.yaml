# Example template for directory-based ingestion.
# The service will scan the specified directory and process all supported file types (e.g., .csv, .avro).
source:
  # Path to the directory containing the source files.
  path: "/Users/skalamani/Downloads/abd"

# The sink specifies where the metadata should be sent.
sink:
  type: datahub
  env: DEV
