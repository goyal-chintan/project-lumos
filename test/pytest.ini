[pytest]
testpaths = test
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
addopts = -ra
markers =
    unit: unit tests
    smoke: lightweight smoke tests (imports / fast sanity checks)
    integration: requires a running DataHub (GMS/UI) and/or external services


